{
    "actorOutputSchemaVersion": 1,

    "title": "Tester MCP Client output",
    "description": "Tester MCP Client provides interactive webpage to chat with LLM and conversation history stored in dataset.",
    "type": "object",

    "properties": {
        "clientUrl": {
            "type": "string",
            "title": "MCP Client",
            "template": "{{run.containerUrl}}"
        },
        "chatHistory": {
            "type": "string",
            "title": "Conversation history",
            "template": "{{links.apiDefaultDatasetUrl}}/items"
        }
    }
}
